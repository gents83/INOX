[package]
name = "inox_messenger"
version = "0.1.0"
authors = ["gents83 <gents83@gmail.com>"]
edition = "2021"
build = "build.rs"

[lib]
crate-type = ["lib"]

[dependencies]
inox_commands = { path = "../commands" }
inox_serialize = { path = "../serialize" }