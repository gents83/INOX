[package]
name = "sabi_serialize_derive"
version = "0.1.0"
authors = ["gents83 <gents83@gmail.com>"]
edition = "2021"

[lib]
proc-macro = true

[dependencies]
syn = {version = "1.0", features = ["full"]}
proc-macro2 = "1.0"
quote = "1.0"
