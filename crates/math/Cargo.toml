[package]
name = "inox_math"
version = "0.1.0"
authors = ["gents83 <gents83@gmail.com>"]
edition = "2021"
build = "build.rs"

[lib]
crate-type = ["lib"]

[dependencies]
cgmath = { path = "../../extern/cgmath", features = ["serde", "swizzle"] }
rand = { path = "../../extern/rand" }